import styled from "styled-components";
import { Button } from "antd";

export const ChatButton = styled(Button)`
  width: 44px;
  height: 44px;
  background: #f2f7ff;
  border: none;
  box-shadow: none;
  border-radius: 7px;
  padding: 6px 0 0 0;
  margin: 0 20px 0 0;

  svg {
    width: 24px;
  }
`;
